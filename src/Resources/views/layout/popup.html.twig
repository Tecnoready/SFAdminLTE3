{# {% from '@SFAdminLTE3/default/ui_elements.html.twig' import flashes as flashes %}
{{ flashes() }} #}
{# No se imprimen los flashes para que cuando se cierre el pop up se impriman en la plantilla base #}

{% set popup = {width: "70%"}|resolve_options(popup|default({}),{  }) %}

{% block content '' %}

    <style>
        .swal2-popup {
            width: {{ popup.width }};
        }
        @media only screen and (max-width: 1000px) {
            .swal2-popup {
                width: 60%;
            }
        }
        @media only screen and (max-width: 800px) {
            .swal2-popup {
                width: 80%;
            }
        }
        @media only screen and (max-width: 600px) {
            .swal2-popup {
                width: 90%;
            }
        }

        {#    z-index: 1060 es el popup #}
        .select2-dropdown {
            z-index: 1061;
        }
        .swal2-actions {
            z-index: 0;
        }
    </style>